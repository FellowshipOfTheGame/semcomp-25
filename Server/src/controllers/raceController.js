/*
 * Player Controllers : Contains all match's endpoints of the current player
 */
const UserModel = require("../models/Player")
const RaceModel = require("../models/Race")
const configEnv = requere("../config")